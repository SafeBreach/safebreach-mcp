# Test Results — SAF-34553

Run date: 2026-08-19 | Branch: bugfix/SAF-34553-disclose-propagate-attacks | HEAD: 7af6a98

## Unit / integration (Automatic)

Command: `uv run pytest safebreach_mcp_playbook/tests/ -m "not e2e" -q`
Result: **306 passed, 0 failed, 34 deselected**

Repo-wide: `uv run pytest safebreach_mcp_config/tests/ safebreach_mcp_data/tests/ safebreach_mcp_utilities/tests/ safebreach_mcp_playbook/tests/ -m "not e2e" -q`
Result: **999 passed, 0 failed, 102 deselected**

| Test | Verdict | Notes |
|------|---------|-------|
| T-1..T-4 | ✅ PASS | discriminator; T-2 (value `0` is Validate) is the case a ui-react port would fail |
| T-5, T-6 | ✅ PASS | `is_propagate` unconditional and truthful |
| T-7..T-10 | ✅ PASS | scope filter incl. case-insensitivity |
| T-11..T-18 | ✅ PASS | default, validation, applied-filters, disclosure, ordering |
| T-19 | ✅ PASS | repro-regression — **confirmed red before the fix** (was among 10 failures at Phase 4 red) |
| T-20..T-23 | ✅ PASS | first tests of the server formatting layer |
| T-25..T-27 | ✅ PASS | by_tags parity; T-27 guards the applied-filters overwrite |
| T-28, T-29 | ✅ PASS | details marker additive |
| T-30 | ⛔ REMOVED | guarded behaviour absent from this repo — see test-plan tombstone |
| T-34 | ✅ PASS | enum + copy exactness |

## E2E (Automatic, real console)

Command: `source .vscode/set_env.sh && uv run pytest safebreach_mcp_playbook/tests/test_e2e.py -k PropagateScopeE2E -v`
Console: read from `E2E_CONSOLE` (a Propagate-capable console; name intentionally not recorded — public repo)
Result: **6 passed, 0 failed in 139.28s**

| Test | Verdict | Observed |
|------|---------|----------|
| T-24 | ✅ PASS | validate=9939, propagate=111, all=10050; 9939+111=10050 coherent; `validate_count`/`propagate_count` agree; no leakage either direction; hint discloses 111 excluded |
| T-24 (pagination) | ✅ PASS | scoped total strictly smaller than `all`; page count follows scope |
| T-31 | ✅ PASS | a live ALM attack flagged through the details path; a live Validate attack not flagged |
| T-32 | ✅ PASS | rendered header figures equal the function-layer counts |
| T-24 (by_tags) | ✅ PASS | applied-filters scope echoed on both default and explicit calls |

### Independent corroboration

The e2e `propagate=111` matches the count measured directly against pentest01's Postgres
(`SELECT count(*) FROM move WHERE tags ? 'ALM'` → 111) via a completely separate path — the
discriminator agrees with the database.

The reporter's own figure reproduces: for the Credential Access tactic, the live run reports
**45 Propagate**, exactly the 45 quoted in SAF-34553. (Validate differs — 136 vs the reported 108 —
because the console carries more content than the reporter's did.)

## Defect found BY the e2e run, not by the unit suite

The disclosure hint rendered as `...page_number=1 111 Propagate (ALM) attacks...` — the paginator's
hint carries no trailing punctuation, so concatenation produced a run-together sentence in the exact
text an LLM is meant to act on. T-17 asserted only that both fragments were *present*, so it passed
on broken output. Fixed in `c900bf4`; T-17 strengthened to assert the boundary. This is the clearest
argument in this ticket against shipping on unit tests alone.

## Not covered

- **T-33 (Manual progression)** — not executed. Requires an AI agent driving the tools against a
  console and judging whether an LLM acts on the disclosure. Outstanding.
- **The Helm customer surface** — out of scope by decision at the Phase 2 gate. Nothing here proves
  Helm's phrasing to the customer improved, only that the data and disclosure reaching it are correct.
- **The no-Propagate-content guard has never been seen firing.** All 6 e2e tests call
  `_require_propagate_content()`, but it has only ever run against a console that HAS content, so the
  failure path itself is unexercised.
