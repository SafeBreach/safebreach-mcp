# GitLeaks configuration to detect secrets
[extend]
useDefault = true

[rules.safebreach-api-token]
description = "SafeBreach API Token"
id = "safebreach-api-token"
regex = '''[a-zA-Z0-9_-]{43}'''
tags = ["key", "SafeBreach"]

[rules.bearer-token]
description = "Bearer Token"
id = "bearer-token"
regex = '''(?i)bearer\s+[a-zA-Z0-9_\-\.=]+'''
tags = ["key", "token"]

[allowlist]
description = "Global allowlist"
commits = []
paths = [
    '''\.secrets\.baseline''',
    '''\.pre-commit-config\.yaml''',
    '''\.vscode/set_env\.sh''',  # Allow dev environment file
    '''safebreach_mcp_server\.egg-info/.*''',  # Allow package info
]
regexes = [
    '''your-secure-token-here''',
    '''your-auth-token-here''',
    '''your-secure-token''',
    '''your-token-here''',
    '''your-token''',
    '''REPLACE_WITH_ACTUAL_TOKEN''',
    '''<your-token-here>''',
    '''<your-api-key>''',
]